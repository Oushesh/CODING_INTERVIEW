#include <iostream>
#include <vector>
#include <map>

class Solution{
public:
  //how to find element of a hash?
  //rewrite a function:
  bool findbyBallue()
  map <int, vector <int>> hash(vector <int> & nums)
  {
    map<int, vector<int>> dict ={};
    //discusting
    for (int i=0;i<nums.size();i++)
    {
      if nums[i]


    }
  };
  vector <vector<int>> permute(vector <int> & nums)
  {
    int pivot = nums[0];
    vector<vector <int>> output;
    for (int i=0;i<nums.size();i++)
    {
      pivot = nums[i];
      rest = []
      //build res here:

    }
  }
};

int main()
{
  vector <int> nums = {1,2,3};
  Solution S1;
  S1.permute(nums);
  return 0;
}
