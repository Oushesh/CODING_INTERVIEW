#include "iostream"
#include "list"

using namespace std;

class Graph
{
public:
    Graph(int V);
};

void Graph(int s)
{
    //Mark all visited versites false
    //Create a Queue for BFS
    //While queue is not empty access the elements
    //All elements that are visited
    bool *visited = new bool[V];

    list<int> queue;
    visited[s] = true;
    qeueue.push_back(s)

    while (!queue.empty())
    {


    }
}